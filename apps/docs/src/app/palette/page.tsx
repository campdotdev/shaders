import { PaletteView } from './PaletteView';

export const metadata = {
  title: 'Palette — Shaders',
  description: 'The brand-aligned color palette used by every Shaders example.',
};

export default function PalettePage() {
  return <PaletteView />;
}
