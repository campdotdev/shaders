---
title: Engine API
description: '@lovo/matter — primitives, runtime, inputs, and reduced-motion-gated time.'
section: reference
order: 10
---

# Engine API

`@lovo/matter` is the framework-agnostic engine. This page lists everything it exports. A generated TypeDoc reference replaces this hand-written page post-launch.

## Tier 2 primitives

TSL building blocks for writing shader expressions. Each primitive is documented individually under [/primitives](/primitives) with a live demo and parameter sliders.

- `colorRamp(stops, t)` — multi-stop color gradient sampled at `t` ∈ [0, 1].
- `simplexNoise(uv, opts?)` — single-octave 2D value noise.
- `fractalNoise(uv, opts?)` — fractional Brownian motion. Octave-stacked noise with `scale`, `octaves`, `lacunarity`, `gain`, `speed`.
- `voronoi(uv, opts?)` — distance-to-nearest-feature noise.
- `quantize(value, bins)` — posterize a float to discrete steps.
- `signedDistanceFieldCircle(uv, radius, center)` — signed-distance field for a circle. Negative inside, positive outside.
- `displace(uv, offset)` — UV displacement helper.
- `cursorRipple(uv, cursor, opts?)` — cursor-following ripple displacement (used by `<DotField>`).

## Layering and `elapsedTime`

Matter sits on top of Three.js's TSL. Pure TSL primitives (`uv`, `vec2`, `vec3`, `vec4`, `uniform`, `mix`, `smoothstep`, `mod`, `sin`, `cos`, `length`, `dot`, `normalize`, `max`, `min`) live in `three/tsl` — import them directly from there:

```ts
import { uv, vec3, uniform, mix } from 'three/tsl'
```

Matter exports one TSL node of its own: **`elapsedTime`**. It's the TSL built-in `time` multiplied by an engine-owned reduced-motion scale uniform, so every component that animates against `elapsedTime` from `@lovo/matter` automatically respects `prefers-reduced-motion` and the policy override set via `setReducedMotionPolicy`.

```ts
import { elapsedTime } from '@lovo/matter'
//  → equivalent to three/tsl's `time.mul(reducedMotionScale)`

// If you specifically want raw uncapped time (e.g. a debug overlay):
import { time as rawTime } from 'three/tsl'
```

> **Migrating from 0.1.x?** Before 0.2.0, `@lovo/matter` re-exported the TSL primitives listed above as a convenience. They're gone in 0.2.0 — replace `import { vec3, uv, … } from '@lovo/matter'` with `import { vec3, uv, … } from 'three/tsl'`. `elapsedTime` (named `time` before 0.5.0) is the only carry-over and stays imported from `@lovo/matter`.

## Runtime

- **`createRenderer(opts)`** → `GpuRenderer`. Allocates a WebGPU renderer. Used internally by `<ShaderScene>`; you rarely call it directly.
- **`FrameScheduler`** — the render-frame coordinator. Components register as `SchedulerClient`s; the scheduler ticks them when their scene is visible and not reduced-motion-paused.
- **Visibility / intersection watchers** — `createVisibilityWatcher()`, `createIntersectionWatcher()`. Low-level utilities behind the offscreen-pause behavior.

## Inputs

- **`CursorInput`** — framework-agnostic cursor source. `useCursor()` in `@lovo/matter-react` wraps it; in Mode 2 or non-React contexts, you instantiate it directly.

## Reduced motion

- **`setReducedMotionPolicy(policy)`** — set globally to `'respect'`, `'slow'`, or `'ignore'`. See [the performance guide](/guides/perf#reduced-motion).
- **`getReducedMotionPolicy()`** — read the current policy.
- **`getReducedMotionTimeScale()`** — get the current animation time scale (0, 0.3, or 1).
- **`createReducedMotionWatcher()`** — subscribe to policy changes.

## Type exports

The engine exports types for everything above: `GpuRenderer`, `CreateRendererOptions`, `GpuBackend`, `SchedulerTick`, `SchedulerClient`, `CursorInputOptions`, `Vector2`, `ColorRampStop`, `TSLNode`, `FractalNoiseOptions`, `CursorRippleOptions`, `ReducedMotionPolicy`, `ReducedMotionWatcher`, `VisibilityWatcher`, `IntersectionWatcher`.

<Callout>
  For React-specific exports (`<ShaderScene>`, hooks, `<FallbackBoundary>`), see [the React API](/react/api). This page only covers `@lovo/matter`.
</Callout>
