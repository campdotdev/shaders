---
'@camp-dev/shaders': minor
---

Add the `createWaveField` runtime and its `WaveField` and `WaveFieldStroke` types. The runtime simulates pointer-driven waves in GPU ping-pong render targets and returns the latest field as a texture for shader composition.
